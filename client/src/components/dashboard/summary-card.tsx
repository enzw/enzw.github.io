import type { LucideIcon } from "lucide-react"
import { Card, CardContent } from "@/components/ui/card"
import { formatCurrency } from "@/lib/format"
export function SummaryCard({ label, value, icon: Icon, tone = "default", hint }: { label: string; value: number; icon: LucideIcon; tone?: "default" | "income" | "expense" | "saving"; hint?: string }) { const tones = { default: "bg-primary/10 text-primary", income: "bg-emerald-500/10 text-emerald-600", expense: "bg-rose-500/10 text-rose-600", saving: "bg-blue-500/10 text-blue-600" }; return <Card><CardContent><div className="flex items-start justify-between gap-3"><div><p className="text-sm text-muted-foreground">{label}</p><p className="finance-number mt-2 text-xl font-semibold sm:text-2xl">{formatCurrency(value)}</p>{hint && <p className="mt-1 text-xs text-muted-foreground">{hint}</p>}</div><span className={`grid size-10 place-items-center rounded-2xl ${tones[tone]}`}><Icon className="size-5" /></span></div></CardContent></Card> }
